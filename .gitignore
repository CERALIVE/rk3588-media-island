# Out-of-tree kernel build scratch — CI clones the pinned kernel here.
.work/

# Kernel module build products.
*.o
*.ko
*.mod
*.mod.c
*.mod.o
*.cmd
.tmp_versions/
Module.symvers
modules.order

# Test and evidence output (Rule D: repository-local, never a path above root).
test-results/

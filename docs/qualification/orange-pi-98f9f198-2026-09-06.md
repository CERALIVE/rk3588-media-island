# Orange Pi 5+ — merged RGA-flip candidate qualification

Date: 2026-09-06, 03:12–04:00 UTC; USB-audio-only follow-up 04:30–04:37 UTC.
Board: **192.168.78.151 only**.

**Verdict: OTA/ownership PASS; full qualification FAIL / incomplete.** HDMI
starts fail in the shipped engine's converter selection. No plan todo was marked,
no PR or release was opened, and no kernel or installed package was patched.
The Rock 5B+ was not contacted.

## Artifact and deployment

| Field | Value |
|---|---|
| Pipeline canonical branch | `master`, freshly fetched and fast-forwarded |
| Pipeline commit | `98f9f198365ffd163bb0e66da65e7f6328ceef3a` (PR #150 merge) |
| Pipeline tree | `9a4f150c8ebdcc58c40a5b78fdc9c18e25b5b390` |
| Kernel source | Linux v7.2, `8d3ae59288f1e7d58d76558a6ee96d533bc5019f` |
| Consumer patches | `365b2463294019e3d4e2d5718a787c2055f4ab59`, island v2026.9.2 |
| Variant | `edge`; `debug_image=false` |
| Signing/layout | Development signer matching the installed NON-PRODUCTION root; bench `x` labels |
| Bundle | `20260906T032203Z.raucb` |
| Bundle SHA-256 | `698cc4a59cdca1542a92ab82fcb50c665afa9d7bbc478a545d1d19c8ffbd2382` |
| Rootfs SHA-256 | `da473550f5e7616cdb3fd31b315eac00821322b6a1dce1f949365d4b890db917` |
| Resolved config SHA-256 | `7f11b1434e48574a9066046fdaf6f64f470ca16d862ca2c99c972f18107df323` |
| New kernel build | `7.2.0-ceralive-rk3588 #ceralive1 SMP PREEMPT @1788664077` |
| Userspace | cerastream `2026.9.1`; ceralive-device `2026.9.0-20260902T184015.b720f33`; plugin `1.14.4+ceralive.1`; librga2 `2.2.0-1` |

The native Linux Docker `default` context was used, not Docker Desktop. The
first invocation stopped before fetching because `ARMBIAN_APT_KEYRING` was
unset. The documented SHA-pinned public keys were supplied for attempt 2; fresh
authenticated package fetching and the normal candidate build then succeeded.
All six dry-run probes had passed (no hardware contact in those probes).
Rootfs size was 1,186,498,560 bytes against 1,500,000,000; package parity was
20 pass / 0 warn / 0 fail. This is **not a production-signed/releasable artifact**.

Build entry (path arguments supplied explicitly):

```sh
DOCKER_CONTEXT=default ci/build-hardware-candidates.sh \
  --only orange-edge --trust-verdict <fresh-trust.json> \
  --signing-env <fresh-signing.env> --evidence <attempt2> \
  --bench-labels 1 --skip-probes
```

The candidate recorder emitted `candidate_is_source_built: command not found`
at line 437. It took the default-variant resolution branch. The default is edge;
an explicit `lib/resolve.sh orange-pi-5-plus --variant edge` confirmed the tuple's
kernel and patch pins. This recorder defect is retained, not repaired or hidden.
The separate build-log census/release gate was **not run**; a successful build
does not certify that every warning has an accepted disposition.

### Full pre-deployment Kconfig gate

Exact command, from the pipeline checkout:

```sh
bash lib/verify-kernel-config.sh \
  --config test-results/opi-pr150-98f9f198-20260906/attempt2/orange-edge.bench-labels-1.config \
  --declared manifests/kernel/rk3588-edge.fragment \
  --required manifests/kernel/required-symbols.list \
  --forbidden manifests/kernel/forbidden-symbols.list
```

Exit 0, output:

```text
ok: 187 of 187 declared symbol(s) survived into the resolved kernel .config (0 reviewed exception(s))
ok: 185 required and 81 forbidden symbol(s) hold in test-results/opi-pr150-98f9f198-20260906/attempt2/orange-edge.bench-labels-1.config
```

```text
# CONFIG_VIDEO_ROCKCHIP_RGA is not set
CONFIG_ROCKCHIP_MPP_SERVICE=m
CONFIG_ROCKCHIP_MPP_RKVENC2=y
CONFIG_ROCKCHIP_MPP_RKVDEC2=y
CONFIG_ROCKCHIP_MPP_JPGDEC=y
CONFIG_ROCKCHIP_MULTI_RGA=m
```

The checked config was extracted from the newly built kernel package. Reading
`./boot/config-7.2.0-ceralive-rk3588` out of the emitted rootfs tar produced the
same config SHA-256. This was not just the positional survival check.

### Before and after

Before: A booted/active, image bundle `b79652b`, manually modified kernel build
`#3 SMP PREEMPT Thu Sep 3 20:11:22 -05 2026`. Both slots good, A=3/B=3.
Root was `/dev/mmcblk1p2`, with `xboot/xrootfs_a/xrootfs_b/xdata` labels.
Existing manually installed `rga_multicore` bindings were inventory, not new proof.

The transferred bundle hash and board-side `rauc info` signature/compatible
were checked before install. CeraUI was idle; its service and the engine were
stopped before `rauc install /tmp/opi-98f9f198.raucb`. Installation succeeded at
03:36:05 UTC, transaction `cb8c127e-af95-4ad2-ac65-0cf6e0ebd1ad`. The shared
mark-good marker was cleared as the installed update wrapper prescribes. A was
confirmed good with budget 3 immediately before reboot; the RAUC journal was
retained before reboot.

After/final: B booted/active/good, A good, `BOOT_ORDER=B A`, A=3/B=3; image-build
marker matches the full pipeline SHA. `systemctl is-system-running` is `running`.
The production image's disabled SSH was enabled via CeraUI Settings → SSH Access.
The original password still authenticated; **no credential changed**.

```text
rga3 fdb60000.rga: probe successfully, irq = 37, hw_version:3.0.76831
rga3 fdb70000.rga: probe successfully, irq = 38, hw_version:3.0.76831
rga2 fdb80000.rga: probe successfully, irq = 102, hw_version:3.2.63318
rga: IOMMU binding successfully, default mapping core[0x1]
```

`/dev/rga` and `/dev/mpp_service` exist. All three RGA bindings point to
`rga_multicore`; mainline rockchip-rga is absent from lsmod. The final kernel
filter found no WARNING/Oops/Call trace/EFAULT. Node/binding success is not a
successful RGA-blit claim: userspace errors below remain real.

## Todo 36 — FAIL before either soak could start

Fresh queries before OTA, after OTA and at final healthcheck all detected
**3840×2160 progressive, 59.94 Hz, 593396000 Hz pixel clock**, total 4400×2250.
`--log-status` reported Power Present `0x00000001`. The initial configured
capture format was 640×480 NV16; that stale format is not the detected timing.
**10-bit depth was not demonstrated.** No absent-source claim is made.

Through CeraUI, selected HDMI Input, 4K, 59.94 fps, 5 Mbps, H.265 first and
H.264 second. A local SRTLA receiver was configured at `192.168.78.228:15000`.
Both attempts failed in under one second:

```text
03:47:11.481Z  H.265  attempt=att_mtp9t6vm_P9AlqKrV
03:47:46.360Z  H.264  attempt=att_mtp9txzh_niQ8QCe
phase=start-rpc class=engine_internal code=-32603 retry=not_retriable
stream start failed internal engine error: no safe V4L2 converter factory matched the platform policy
```

`gst-inspect-1.0 rgaconvert` reports no such element. This records the observed
selection failure and missing element, not a proven root-cause fix prescription.
Neither planned 30-minute session began. No 6h/24h/72h tier was attempted.

## Todo 37 — inventory complete, functional matrix incomplete

**The initial USB-audio absence below is historical.** The operator attached a
DJI MIC MINI afterwards; its actual capture and selectable UI row passed in the
USB-audio-only follow-up at the end of this document. Video and HDMI validation
were not repeated by that follow-up.

The requested YAML was absent, so this run creates the ten-class record at
[`tests/board/usb-matrix.yaml`](../../tests/board/usb-matrix.yaml), explicitly
distinguishing inventory from a functional PASS. No UVC/UAC interface is
enumerated: USB interface classes are 09 (hubs) and e0 (MediaTek wireless).
Non-root devices are `05e3:0610`, `05e3:0620`, `0e8d:0717`. Connected Bluetooth
device inventory is empty. No camera or microphone is inferred from a radio.

HDMI audio is present: ALSA hdmirx capture was RUNNING, S32_LE, 48000 Hz,
**2 channels**, owned by PipeWire PID 549. Receiver metadata independently showed
AAC-LC, 48 kHz stereo. This is neither eight-channel proof, a fidelity measurement,
nor an A/V-sync result. UAC/HFP rows remain NO-DEVICE; the audio-class qualification
is FAIL/incomplete because the required axes were not all exercised.

## Todo 38 — partial component observations, full-island requirement NOT PASSED

The fallback product source was explicitly **synthetic Test Pattern**, 1080p30
H.264, with actual HDMI stereo audio. Host ffmpeg received MPEG-TS H.264 High,
yuv420p, 1920×1080, 30 fps plus AAC stereo through SRTLA. This is not physical
video capture. RGA repeatedly logged `RGA_BLIT fail: Invalid argument`; observed
RGA and decoder load stayed zero. Full-island workload admission therefore fails.

| Drill | Observation / verdict |
|---|---|
| SIGKILL during synthetic product stream | `systemctl kill --kill-whom=main --signal=KILL cerastream.service`; PID 942 → 21950, NRestarts 0 → 1; exited 03:51:11, restarted 03:51:17. Supervisor restart demonstrated; automatic stream resume not demonstrated. |
| KillMode interplay | `KillMode=process`. UI stayed STREAMING but showed 0 kbps and “No video reaching your stream”; manual UI Stop/Start was required for the next trial. No healthy-resumed claim. |
| SIGSEGV during next synthetic stream | `systemctl kill --kill-whom=main --signal=SEGV cerastream.service` at 03:53:04 returned successfully. At 03:53:19 PID remained 21950, NRestarts remained 1, encoder still loaded. **Crash/restart not induced**, not PASS. |
| 20 teardown cycles | Twenty finite synthetic NV12 720p H.264 pipelines each exited 0, followed by two concurrent processes terminated mid-encode. Narrow component result only. |
| Two-process concurrent destruction | Both were alive after 3 seconds; both exited with 143 after TERM. Subsequent 30-frame encode exited 0. Not encoder+decoder+RGA composition. |
| Leak/wedge check | Before/after: queue depth 0, exported dma-bufs 0 objects/0 bytes, empty MPP sessions summary; same idle engine RGA session. No change on these surfaces; no general kernel-heap leak proof. |
| Loaded-island network disconnect/reconnect, ADR-0009 | NOT RUN: full-island live-capture admission failed; management uplink was not disrupted to manufacture a result. |
| Watchdog-triggered recovery | NOT RUN. Watchdog configured 90 s; SIGKILL restart is not watchdog evidence. |

The signal/teardown trials are intentionally short fault-injection operations,
not shortened soak passes. Synthetic trials were stopped after recording errors.

## Todo 42 — short samples only, full benchmark BLOCKED

Six **unpaced synthetic NV12** 300-buffer samples at 8 Mbps, with parsed AU
counting and Bash process-tree timing. Each emitted exactly 300 AUs and exited 0.
These include source generation and startup/teardown; they are **not maximum or
sustained throughput, captured-video measurements, or 30-minute sessions**.

| Codec | Requested caps | AUs | Wall s | AU/s over whole process | CPU % (one core = 100%) |
|---|---|---:|---:|---:|---:|
| H.264 | 3840×2160 60/1 | 300 | 5.080 | 59.06 | 172.97 |
| H.264 | 3840×2160 30/1 | 300 | 5.073 | 59.14 | 174.91 |
| H.264 | 1920×1080 60/1 | 300 | 1.633 | 183.71 | 152.50 |
| H.265 | 3840×2160 60/1 | 300 | 5.080 | 59.06 | 180.24 |
| H.265 | 3840×2160 30/1 | 300 | 5.075 | 59.11 | 182.20 |
| H.265 | 1920×1080 60/1 | 300 | 1.609 | 186.45 | 157.07 |

The initial sample invocation mistakenly used the old plugin's `bps` property:
all six failed pipeline parsing with zero AUs. Those logs remain retained. Live
`gst-inspect` established the new plugin uses `bitrate` (BPS), and only the
corrected v2 run supplies the numbers above. No device fix was made.

Todo-3 baseline (2026-09-02, same board) recorded synthetic 600-AU 4K59.94 H.264
at 63.16 fps and H.265 at 61.79 fps, and two live 4K30 H.265 processes at 30.01
fps each. It recorded H.264 PSNR CLEAN and H.265 DIRTY (38.358 dB outlier), plus
software H.265/MJPEG decode. None of those oracle/decode findings is closed by
these new encode-only samples.

Current 4K samples are numerically lower, but different buffer counts, frame-rate
caps, plugin versions and startup accounting prevent an apples-to-apples regression
claim or attribution to the island. **Regression explanations are unresolved.**
No replacement baseline was written. Missing full-suite axes: sustained maxima,
2×4K/4×1080p/8×720p throughput, capture→AU latency, per-core RGA throughput,
time-resolved RKVENC utilization, thermal soak, drops/jitter and output-quality
verification. Post-process thermal/load snapshots are retained but do not fill
those measurement gaps.

## Evidence and end state

Repo-local raw evidence: `test-results/opi-pr150-98f9f198-20260906/` (gitignored).
It contains build/config/tuple records, before/after/RAUC transcripts, both HDMI
start failures, USB/audio inventory, receiver logs, signal trials, individual
teardown logs/snapshots, exact sample commands, logs and timings, and the retained
todo-3 baseline copy. No credentials are included in this document or the matrix.

Final 04:00:07 UTC: candidate B remains active/good, A good, A=3/B=3, system
running, engine active/idle, NRestarts=1 from the deliberate KILL, media loads and
queue depth zero, exported dma-bufs zero; no remaining gst-launch or srtla_send
process found. The host receiver was stopped. SSH remains enabled and credentials
unchanged. UI source is restored to Test Pattern, 1080p30, Auto codec, 5 Mbps.
The custom bench receiver address remains configured: the UI refuses saving the
original unconfigured Cloud destination without a key. **Do not start a stream
without choosing a real receiver.** No source hotplug or network disruption was
performed, and no ≥30-minute successful soak is claimed.

## USB-audio-only follow-up — 04:30–04:37 UTC

The operator reported new external microphone attachment after the original
inventory. A fresh USB/ALSA/PipeWire scan at **04:30:25 UTC** found exactly **one
USB audio device**, not an assumed number of physical transmitters:

| Identity | Observed value |
|---|---|
| Product/manufacturer | DJI MIC MINI / DJI Technology Co., Ltd. |
| VID:PID | `2ca3:4011` |
| USB topology/speed | `1-1.4`, full speed / 12 Mb/s |
| ALSA card | index 4, id `usbaudio`, name `DJI MIC MINI` |
| Capture device | `pcmC4D0c`, stable card selector `hw:CARD=usbaudio` |
| Advertised USB capture | Interface 3, altsetting 1, endpoint `0x81`, S24_3LE, 24 bits, stereo, **48000 Hz only** |
| PipeWire source | `alsa_input.usb-DJI_Technology_Co.__Ltd._DJI_MIC_MINI_XSP12345678B-01.analog-stereo` |

`lsusb` and `arecord` are absent; neither was installed for this check. USB
identity came from `/sys/bus/usb/devices`, with `/proc/asound/cards`,
`/proc/asound/pcm`, `/proc/asound/card4/stream0`, and `pw-cli ls Node` providing
the audio join. CeraUI's documented convention preserves the ALSA card id on
the PipeWire arm and uses the kernel card scan for identity/naming.

### Genuine capture result — PASS for 48 kHz stereo

At **04:34:42 UTC**, recorded this exact PipeWire source, not the default source:

```sh
PIPEWIRE_RUNTIME_DIR=/run/pipewire \
  timeout --signal=INT --kill-after=3s 6s pw-record --verbose \
    --target alsa_input.usb-DJI_Technology_Co.__Ltd._DJI_MIC_MINI_XSP12345678B-01.analog-stereo \
    --rate 48000 --channels 2 --format s32 <private-temporary>/mic.wav
```

While recording, the **real ALSA device** reported RUNNING, MMAP_INTERLEAVED,
**S24_3LE / 48000 Hz / 2 channels**; USB stream0 independently reported Running,
interface 3/altsetting 1 and momentary frequency 48000 Hz. PipeWire reported its
stream transitioned to `streaming`, with sample ticks advancing.

The intentional six-second SIGINT yields timeout exit **124**; that code is not
used as proof of success. The resulting finalized WAV is independently readable:

```text
ffprobe: pcm_s32le, sample_rate=48000, channels=2, bits_per_sample=32
duration=5.973333 seconds, size=2293804 bytes
ffmpeg astats: Number of samples=286720 (per channel)
Peak level dB=-27.172505; RMS level dB=-41.846446
```

The 32-bit WAV is the requested application representation of the device's
24-bit samples, not a 32-bit hardware claim. Nonzero varying PCM was measured;
no listening, acoustic-fidelity, transmitter-count, independent-stereo-feed,
long-duration or A/V-sync claim follows. Both channels produced identical
statistics. The private temporary WAV was deleted after analysis; only logs and
metadata were retained.

Earlier attempts remain in the evidence: the first command had a shell-quoting
error and did not capture; the next GStreamer `pipewiresrc num-buffers=500`
attempt negotiated capture but hit its 20-second deadline and left an unreadable
WAV after TERM. Neither is counted as a PASS or concealed as device absence.
The explicit device-targeted `pw-record` run above supplies the valid capture.

### CeraUI selector — PASS without changing selection

At **04:36:20 UTC**, the Live audio-source menu contained the enabled option
**“DJI MIC MINI Analog Stereo”**, badge **External**. Playwright observed
`enabled=true`, no `aria-disabled` value. The existing selected audio source
remained **HDMI Input**; Escape closed the menu. No audio config was saved and
no video preview or stream was started. Screenshot: `usb-audio-selector.png`.

The matrix now has `uac_48k: PASS` and a device-specific PASS row with these
identifiers, format, capture results and UI evidence. The overall audio class
remains incomplete: **no native 96 kHz device is available** (this device offers
48 kHz only), HFP remains untested/previously NO-DEVICE, and HDMI eight-channel
validation is outside this follow-up. No updates to todos 36/38/42 are implied.

### Final state and evidence

At **04:37:53 UTC**: same `98f9f198` image, B active/good, A good, budgets
A=3/B=3, system `running`, engine PID **21950** and `NRestarts=1` unchanged from
preflight, all MPP loads zero. No `pw-record`, `gst-launch` or `srtla_send`
process remained, and no temporary microphone WAV remained. The DJI ALSA card
was still enumerated. No service restart, reboot, package installation,
credential change or stream start occurred in this follow-up.

Raw files under `test-results/opi-pr150-98f9f198-20260906/`:
`usb-audio-followup-inventory.txt`, `usb-audio-followup-capture.txt` (failed
command), `usb-audio-followup-capture-v2.txt` (unfinalized WAV),
`usb-audio-followup-capture-v3.txt` (valid capture), `usb-audio-capture.sh`,
`usb-audio-selector.png`, and `usb-audio-followup-final-health.txt`.
Changes remain uncommitted on the existing evidence branch for consolidation.

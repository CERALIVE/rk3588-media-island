# RGA host verification — 465598e26

Measured 2026-09-15 UTC. Target: `465598e2667a36b067a15d0ef8ab490b68ef4f7a`
on `fix/rga-table-ownership-memory-routing`; parent: `bb52499a3`.
This is execution evidence, not a second correctness review or board qualification.
No production driver, test, generated series, pin or release was changed.

## KUnit: 47 RGA-related cases executed and passed

Built a fresh UML kernel from the pinned kernel commit
`8d3ae59288f1e7d58d76558a6ee96d533bc5019f` (Linux 7.2), using GCC
`16.2.1 20260810` and Python `3.14.7`. Staged the target's current sources with
`tests/kunit/stage_ioctl.py`, preserving the selected production function bodies.
The staging self-test passed. Used the unchanged `tests/kunit/.kunitconfig`.

Reproduction, with `KERNEL_TREE`, `BUILD_DIR`, `RESULT_JSON` and `TMPDIR`
pointing to fresh repo-local scratch locations:

```sh
python3 tests/kunit/stage_ioctl.py --self-test
python3 tests/kunit/stage_ioctl.py "$KERNEL_TREE"
python3 "$KERNEL_TREE/tools/testing/kunit/kunit.py" run \
  --kunitconfig="$PWD/tests/kunit" --build_dir="$BUILD_DIR" \
  --jobs=12 --timeout=300 --json="$RESULT_JSON"
```

The build/run was detached. It exited **0** after 31.202 seconds. The generated
kernel really booted with `kunit.enable=1 mem=1G console=tty kunit_shutdown=halt`.
Counts below come from per-case `ok` records in the raw kernel KTAP, checked
against the runner's parsed results, not just the command's return status.
Suite-summary `ok` records are not counted as cases.

| Suite | Passed cases | Failed | Skipped |
|---|---:|---:|---:|
| `rockchip-rga-fence` | 1 | 0 | 0 |
| `rga-validate` | 20 | 0 | 0 |
| `rockchip-rga-fault-injection` | 8 | 0 | 0 |
| `rockchip-rga-fault-disabled` | 1 | 0 | 0 |
| `rockchip-rga-ioctl` | 8 | 0 | 0 |
| `rockchip-rga-runtime-pm` | 7 | 0 | 0 |
| **Dedicated RGA suites** | **45** | **0** | **0** |

Two additional executed RGA cases in `rockchip-media-dma-policy` bring the
RGA-related count to **47**:

```text
ok 3 rga_staging_segment_limit_honours_device_maximum_test
ok 4 rga_dma_capabilities_preserve_command_buffer_width_test
```

The full configuration registered 111 cases in 16 suites. **110 passed; 1
skipped; 0 failed.** The runner says `Ran 111 tests: passed: 110, skipped: 1`;
that is **110 non-skipped executions**, not 111 passing executions. The existing
MPP `mpp_fault_idle_enqueue_races_disable_test` self-skipped with
`forced interleaving requires two online CPUs` on single-CPU UML. Its concurrent
interleaving is **not proven** here. No skip was added or test weakened.

In particular, `rga_fault_concurrent_consume_test` has an actual passing KTAP
case record; it is not an undiscovered test behind a vacuous shell predicate.

### Coverage boundary and evidence custody

These are the existing KUnit helper/direct-handler suites. This run does not
turn the separate host ring-overlap, high-memory-import, routing or USERPTR
staging reproducers into KUnit tests; no new memory-repair KUnit case was added
by the target commit. Physical DMA, exporter behavior, pixels, stress and board
qualification remain separate gates. The established reset fix and eliminated
cross-core hypothesis are not reopened.

Raw local artifacts are retained in the ignored `.work/verification-465598/`:

| Artifact | SHA-256 |
|---|---|
| `kunit/test.log` (raw boot/KTAP) | `0f22b2df99bbabd3400cf341c9e9375ca86ede37ba01708f876545ad2532f8bb` |
| `kunit/.config` | `7dc6b9dd2907b25a6e248dbb64d881933f82f7c462e297ae56f2bf94aa0ed41f` |
| `kunit.json` | `1e865f84f40a4f099edf482698a549db0d8d80ded037e91356de5625d2e14a43` |

## Remaining host checks at this checkpoint

Coccinelle scan and parent-versus-target MPP hardening comparison are still in
progress. This checkpoint claims only the measured KUnit result above.
